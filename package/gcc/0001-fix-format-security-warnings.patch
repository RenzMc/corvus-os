--- a/libcpp/expr.cc
+++ b/libcpp/expr.cc
@@ -839,7 +839,7 @@ _cpp_eval_expression (cpp_reader *pfile, const cpp_token *temp)
       if (result == CPP_EOF)
 	{
 	  if (pfile->state.skipping)
-	    cpp_error (pfile, CPP_DL_ERROR,
+	    cpp_error (pfile, CPP_DL_ERROR, "%s",
 		       "unterminated #if");
 	  return false;
 	}
@@ -842,7 +842,7 @@ _cpp_eval_expression (cpp_reader *pfile, const cpp_token *temp)
 	  if (result == CPP_EOF)
 	    {
 	      if (pfile->state.skipping)
-		cpp_error (pfile, CPP_DL_ERROR,
+		cpp_error (pfile, CPP_DL_ERROR, "%s",
 			   "unterminated #if");
 	      return false;
 	    }
@@ -852,7 +852,7 @@ _cpp_eval_expression (cpp_reader *pfile, const cpp_token *temp)
       if (temp->type == CPP_EOF)
 	{
 	  if (pfile->state.skipping)
-	    cpp_error (pfile, CPP_DL_ERROR,
+	    cpp_error (pfile, CPP_DL_ERROR, "%s",
 		       "unterminated expression");
 	  return false;
 	}
@@ -864,7 +864,7 @@ _cpp_eval_expression (cpp_reader *pfile, const cpp_token *temp)
 	  if (temp->type == CPP_EOF)
 	    {
 	      if (pfile->state.skipping)
-		cpp_error (pfile, CPP_DL_ERROR,
+		cpp_error (pfile, CPP_DL_ERROR, "%s",
 			   "unterminated expression");
 	      return false;
 	    }
@@ -867,7 +867,7 @@ _cpp_eval_expression (cpp_reader *pfile, const cpp_token *temp)
 	    if (temp->type == CPP_EOF)
 	      {
 		if (pfile->state.skipping)
-		  cpp_error (pfile, CPP_DL_ERROR,
+		  cpp_error (pfile, CPP_DL_ERROR, "%s",
 			     "unterminated expression");
 		return false;
 	      }
@@ -874,7 +874,7 @@ _cpp_eval_expression (cpp_reader *pfile, const cpp_token *temp)
       if (temp->type == CPP_EOF)
 	{
 	  if (pfile->state.skipping)
-	    cpp_error (pfile, CPP_DL_ERROR,
+	    cpp_error (pfile, CPP_DL_ERROR, "%s",
 		       "unterminated expression");
 	  return false;
 	}

--- a/libcpp/macro.cc
+++ b/libcpp/macro.cc
@@ -182,7 +182,7 @@ _cpp_builtin_macro_text (cpp_reader *pfile, cpp_hashnode *node,
       if (CPP_OPTION (pfile, Traditional))
 	{
 	  if (pfile->state.skipping)
-	    cpp_error (pfile, CPP_DL_ERROR,
+	    cpp_error (pfile, CPP_DL_ERROR, "%s",
 		       "C++ style comments are not allowed in traditional C");
 	  return "";
 	}
@@ -211,7 +211,7 @@ _cpp_builtin_macro_text (cpp_reader *pfile, cpp_hashnode *node,
       if (CPP_OPTION (pfile, Traditional))
 	{
 	  if (pfile->state.skipping)
-	    cpp_error (pfile, CPP_DL_ERROR,
+	    cpp_error (pfile, CPP_DL_ERROR, "%s",
 		       "C++ style comments are not allowed in traditional C");
 	  return "";
 	}
@@ -3710,7 +3710,7 @@ _cpp_expand_definition (cpp_reader *pfile, cpp_macro *macro)
   if (macro->count == 0)
     {
       if (pfile->state.skipping)
-	cpp_error (pfile, CPP_DL_ERROR,
+	cpp_error (pfile, CPP_DL_ERROR, "%s",
 		   "macro &quot;%s&quot; requires %d arguments, but only %d given",
 		   NODE_NAME (macro->paramc), macro->paramc, 0);
       return false;
@@ -3725,7 +3725,7 @@ _cpp_expand_definition (cpp_reader *pfile, cpp_macro *macro)
       if (pfile->state.skipping)
 	{
-	  cpp_error (pfile, CPP_DL_ERROR,
+	  cpp_error (pfile, CPP_DL_ERROR, "%s",
 		     "macro &quot;%s&quot; passed %d arguments, but takes just %d",
 		     NODE_NAME (macro->paramc), macro->paramc, macro->paramc);
 	  return false;
--- a/gcc/gimple-range-gori.cc
+++ b/gcc/gimple-range-gori.cc
@@ -315,7 +315,7 @@ gimple_range_gori::gori_compute (range_op_float &handler,
   if (!base)
     {
       if (dump_file)
-	fprintf (dump_file,
+	fprintf (dump_file, "%s",
 		 "Failed to calculate a range for %s types %s and %s\n",
 		 type_to_string (type1), type_to_string (type2));
       return false;
--- a/gcc/tree-ssa-sccvn.cc
+++ b/gcc/tree-ssa-sccvn.cc
@@ -283,7 +283,7 @@ vn_nary_op_insert_pieces (unsigned int length,
 	{
 	  if (dump_file && (dump_flags & TDF_DETAILS))
 	    fprintf (dump_file,
-		     "Found to be defined with value ");
+		     "%s", "Found to be defined with value ");
 	  vn_nary_op_t result;
 	  result.type = result_type;
 	  result.opcode = opcode;
--- a/gcc/diagnostic.cc
+++ b/gcc/diagnostic.cc
@@ -2330,7 +2330,7 @@ diagnostic_set_info_translated (diagnostic_info *diagnostic,
     {
       if (diagnostic->kind == DK_PERMERROR)
 	{
-	  pp_printf (context->printer,
+	  pp_printf (context->printer, "%s",
 		     "warning: %s\n", message);
 	}
       else